# Backend Server Configuration
# Required for the backend server to access Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Backend server port (defaults to 3001)
PORT=3001

# Frontend Configuration
# Optional: Backend API URL (defaults to http://localhost:3001)
VITE_API_URL=http://localhost:3001

# Optional: Enable chatbot with client-side API key (NOT RECOMMENDED FOR PRODUCTION)
# WARNING: Setting this exposes your API key to the client browser
# Only use for development/testing. Leave empty to disable chatbot feature.
# VITE_GEMINI_API_KEY=your_gemini_api_key_here
